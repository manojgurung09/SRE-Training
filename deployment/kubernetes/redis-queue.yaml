apiVersion: apps/v1
kind: Deployment
metadata:
  name: bharatmart-redis-queue
  namespace: bharatmart
  labels:
    app: bharatmart
    tier: queue
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bharatmart
      tier: queue
      component: redis
  template:
    metadata:
      labels:
        app: bharatmart
        tier: queue
        component: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        command:
        - redis-server
        - --appendonly
        - "yes"
        - --databases
        - "16"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        volumeMounts:
        - name: redis-queue-data
          mountPath: /data
        livenessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: redis-queue-data
        persistentVolumeClaim:
          claimName: redis-queue-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: bharatmart-redis-queue
  namespace: bharatmart
  labels:
    app: bharatmart
    tier: queue
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: bharatmart
    tier: queue
    component: redis
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-queue-pvc
  namespace: bharatmart
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: oci-bv
