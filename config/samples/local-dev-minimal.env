# ============================================================================
# LOCAL DEVELOPMENT - MINIMAL (Zero Dependencies)
# ============================================================================
# Perfect for: Learning, quick testing, offline development
# Setup time: 1 minute
# Monthly cost: $0
# Internet required: No
# External services: None
#
# Usage:
#   1. cp config/samples/local-dev-minimal.env .env
#   2. npm install
#   3. npm run dev -- --host 0.0.0.0 --port 5173 (frontend) + npm run dev:server (backend)
#
# Guide: DEPLOYMENT_QUICKSTART.md#scenario-1-local-development
# ============================================================================

# Deployment Configuration
DEPLOYMENT_MODE=single-vm
NODE_ENV=development

# Database - Supabase (managed PostgreSQL)
DATABASE_TYPE=supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Authentication
AUTH_PROVIDER=supabase
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# Workers - In-process (no external queue needed)
# Jobs execute synchronously in the same process
WORKER_MODE=in-process
# Note: No QUEUE_REDIS_URL needed for in-process workers

# Cache - Memory (no Redis needed)
# Data stored in memory, lost on restart
CACHE_TYPE=memory
# Note: No CACHE_REDIS_URL needed for memory cache

# Secrets Management
SECRETS_PROVIDER=env

# Server Configuration
PORT=3000
HOST=localhost
FRONTEND_URL=http://localhost:5173

# Logging
LOG_LEVEL=debug
LOG_FILE=./logs/api.log

# Metrics
ENABLE_METRICS=true

# Frontend Configuration (for Vite)
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=BharatMart
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin Seed (for db:init script)
ADMIN_EMAIL=admin@bharatmart.com
ADMIN_PASSWORD=Admin@123

# Observability (optional - minimal dev doesn't need)
# OTEL_SERVICE_NAME=bharatmart-backend
# OTEL_EXPORTER_OTLP_ENDPOINT=

# Chaos Engineering (optional - disabled for minimal dev)
CHAOS_ENABLED=false
CHAOS_LATENCY_MS=0
